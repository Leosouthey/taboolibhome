export const metadata = {
  title: "TabooLib Quickstart",
  description: "TODO",
};

export default function QuickstartLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
