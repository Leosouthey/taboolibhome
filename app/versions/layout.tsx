export const metadata = {
  title: "TabooLib Versions",
  description: "TODO",
};

export default function QuickstartLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
